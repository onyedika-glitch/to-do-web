{
  "name": "laravel-todo-modern-ajax",
  "private": true,
  "scripts": {
    "dev": "echo 'No build needed - CSS/JS embedded in layout'",
    "build": "echo 'No build needed - CSS/JS embedded in layout'"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
